function process(input: string) {
	const value = input;
	console.log(value);
}
const data = "hello";
process(data);

const greet = (name: string) => {
	console.log(name);
};
const user = "Alice";
greet(user);
